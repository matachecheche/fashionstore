# URL de la API comercial. Si se deja vacía, la app la deduce de la IP de tu PC (la misma con que Expo Go se conecta) + puerto 3000.
# iniciar.py la escribe automáticamente en mobile-app/.env
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
